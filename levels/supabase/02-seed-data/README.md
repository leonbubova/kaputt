# seed it
Ticket: "Every dev gets an empty `notes` table after `supabase db reset`. Add `supabase/seed.sql` (in `~/.k8s-wargame/supabase/project/`) inserting at least 3 notes, and make sure it's actually loaded."
Goal: `seed.sql` exists and `public.notes` has ≥ 3 rows.
