# publishing a post fails with a weird 400
"Every `POST /rest/v1/posts` (service key, `{"title":…,"body":…}`) fails: `record "new" has no field …`. It worked before the slug feature shipped." Goal: posts can be inserted and get a slug like `hello-world-again` for title `Hello World, Again`. Don't drop the feature.
