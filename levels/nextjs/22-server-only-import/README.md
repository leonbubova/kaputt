# /products: "Module not found: Can't resolve 'fs'"
"A colleague made the product list load its own data so the page component stays tiny. Since then `/products` is a 500 with a webpack error about `fs`. The filter box on that page has to keep working."
App: `~/.k8s-wargame/nextjs/app`. Goal: `/products` renders all three products; the filter input stays client-side.
