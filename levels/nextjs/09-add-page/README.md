# add an /about page
"The nav already links to `/about` but it 404s. Marketing wants a simple page there: a heading `About` and one sentence."
App: `~/.k8s-wargame/nextjs/app` — `npm run dev` (port 3100), `npm run build`, `npm test`. Goal: `GET /about` returns 200 with an `<h1>About</h1>`.
