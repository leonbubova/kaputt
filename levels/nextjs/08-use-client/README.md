# dashboard is a 500
"`/dashboard` shows the red Next.js error overlay since the counter widget was 'cleaned up'. Every other page works."
App: `~/.k8s-wargame/nextjs/app`. Goal: `GET /dashboard` renders 200 with the counter widget (`data-testid="counter"`) still interactive.
