# shop name flashes then disappears in the browser
"The header shows the shop name for a split second, then it's empty, and the console has a hydration warning. `curl` even shows the name in the HTML. The name comes from an env var and the header is a Client Component (it gets a theme toggle next sprint — keep it one)."
App: `~/.k8s-wargame/nextjs/app`. Goal: the shop name `Wurm Shop` is rendered by the client component *and* present in the browser bundle.
