## 1
The dev server (`npm run dev`) shows whatever is in the files right now. Change the file, save, reload the browser.
## 2
The file is `~/.k8s-wargame/nextjs/app/src/app/page.tsx`. The line to change is `<h1>Welcome</h1>` — only the word between the tags changes.
## 3
In `src/app/page.tsx` replace the line with:
    <h1>Hello</h1>
