`(name)` folders are route groups — omitted from the URL.
    mkdir -p 'src/app/(legal)/imprint' && cat > 'src/app/(legal)/imprint/page.tsx'   # export default function Imprint() { return <h1>Imprint</h1> }
