# browser tab shows no title
"Since yesterday the shop's tab just says `localhost:3100`. SEO complains the `<title>` and description are gone from every page. No build error, nothing in the logs."
App: `~/.k8s-wargame/nextjs/app`. Goal: `GET /` contains `<title>Wurm Shop</title>`.
