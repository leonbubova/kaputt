# the stock mail is full of "Product(sku=…).name"
"Since the report line was 'tidied up' every row reads like `Product(sku=A1, name=cable, …).sku  Product(…).name  €19.99  stock=Product(…).stock`." Goal: rows look like `A1  cable  €19.99  stock=12`.
App: ~/.k8s-wargame/kotlin/app · `./run.sh` (compile + demo) · `wg check` compiles src/ with a hidden check
