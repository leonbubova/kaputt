## 1
`val age = 36` — no quotes, it is a number. A box is declared once, then used by name.
## 2
Inside quotes, `$age` is replaced by the content of the box: `"$name is $age"`.
## 3
```
val name = "Ada"
val age = 36
println("$name is $age")
```
