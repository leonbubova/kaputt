# restock is a no-op
"Warehouse books 10 adapters in, `restock("B2", 10)` logs the movement, but the stock stays at 3. No error anywhere." Goal: after `restock("B2", 10)` the product reports stock 13 and the log has the restock entry.
App: ~/.k8s-wargame/kotlin/app · `./run.sh` (compile + demo) · `wg check` compiles src/ with a hidden check
