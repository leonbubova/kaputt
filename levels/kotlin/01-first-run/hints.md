## 1
Only the text between the two quotes is printed. Change `world` to `kotlin`, save, `./run.sh` again.
## 2
Every edit needs a save before `./run.sh` sees it. A red `error:` line means a quote or bracket went missing — compare with the original line.
## 3
```
fun main() {
    println("hello, kotlin")
}
```
