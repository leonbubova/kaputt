# CI needs a script whose exit code says even or odd
"A pipeline step must fail on odd numbers. Write `even.sh N`: print `even` and exit 0 when N is even, print `odd` and exit 1 when it is odd."
Goal: `even.sh 4` prints `even` and exits 0; `even.sh 7` prints `odd` and exits 1.
