# we need a doubler for the pricing sheet
"Finance keeps doubling amounts by hand. Write `double.sh N`: print N multiplied by 2 — as a number, not the text `N*2`."
Goal: `double.sh 21` prints `42`; `double.sh 0` prints `0`.
