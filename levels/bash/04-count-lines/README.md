# how many lines in this export?
"Ops drops a data file in the sandbox and wants a quick line counter. Write `count-lines.sh FILE`: print the number of lines in FILE."
Goal: a file `data.txt` (5 lines) is present; `count-lines.sh data.txt` prints `5`.
