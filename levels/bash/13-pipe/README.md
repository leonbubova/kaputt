# how many errors are in the log?
"Same log, different question: the dashboard wants one number. Write `error-count.sh FILE`: print how many lines of FILE contain `ERROR`. Do it in one line — feed the errors straight into the counter you already know."
Goal: `error-count.sh app.log` prints `3`.
