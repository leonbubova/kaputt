# greeter should not print 'hello, ' when called with no name
"Reuse the greeter idea, but make it friendly: `greet2.sh [name]` greets the given name, and falls back to `world` when called with no argument."
Goal: `greet2.sh` prints `hello, world`; `greet2.sh Bob` prints `hello, Bob`.
