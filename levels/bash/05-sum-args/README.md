# add up a variable list of numbers
"A billing helper needs to total however many amounts it is given. Write `sum.sh`: it prints the sum of all its integer arguments, and prints `0` when given none."
Goal: `sum.sh 2 3 4` prints `9`; `sum.sh` prints `0`.
