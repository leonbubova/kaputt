# the report prints 'total: ' with no number
"`report.sh` should print `total: 42`. It prints `total: ` with the value missing. The number is computed — it just never makes it into the output."
Goal: `report.sh` prints exactly `total: 42`.
