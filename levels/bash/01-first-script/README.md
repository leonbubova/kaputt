# we need a tiny greeter script for the onboarding docs
"New hires copy-paste a `greet.sh` from the wiki, but the file is gone. Recreate it: it takes a name as its first argument and prints `hello, <name>`."
Goal: `~/.k8s-wargame/bash/work/greet.sh` exists and `greet.sh Ada` prints exactly `hello, Ada`.
