# clients get a certificate-name error for shop.local
HTTPS is up, but verifying clients reject it: the served certificate is issued for a different
hostname, so it doesn't match shop.local. Serve the correct certificate (and its key) for shop.local.
