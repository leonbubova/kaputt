# the roles allow-list is missing — the app can't check permissions
Authorization checks whether a user's role is a member of the set `roles`. The key doesn't exist.
Create it as a set with exactly the members: admin, editor, viewer.
