// shop.local frontend bundle
function render1(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 1'; return state; }
function render2(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 2'; return state; }
function render3(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 3'; return state; }
function render4(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 4'; return state; }
function render5(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 5'; return state; }
function render6(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 6'; return state; }
function render7(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 7'; return state; }
function render8(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 8'; return state; }
function render9(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 9'; return state; }
function render10(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 10'; return state; }
function render11(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 11'; return state; }
function render12(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 12'; return state; }
function render13(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 13'; return state; }
function render14(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 14'; return state; }
function render15(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 15'; return state; }
function render16(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 16'; return state; }
function render17(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 17'; return state; }
function render18(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 18'; return state; }
function render19(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 19'; return state; }
function render20(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 20'; return state; }
function render21(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 21'; return state; }
function render22(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 22'; return state; }
function render23(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 23'; return state; }
function render24(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 24'; return state; }
function render25(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 25'; return state; }
function render26(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 26'; return state; }
function render27(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 27'; return state; }
function render28(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 28'; return state; }
function render29(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 29'; return state; }
function render30(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 30'; return state; }
function render31(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 31'; return state; }
function render32(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 32'; return state; }
function render33(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 33'; return state; }
function render34(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 34'; return state; }
function render35(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 35'; return state; }
function render36(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 36'; return state; }
function render37(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 37'; return state; }
function render38(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 38'; return state; }
function render39(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 39'; return state; }
function render40(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 40'; return state; }
function render41(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 41'; return state; }
function render42(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 42'; return state; }
function render43(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 43'; return state; }
function render44(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 44'; return state; }
function render45(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 45'; return state; }
function render46(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 46'; return state; }
function render47(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 47'; return state; }
function render48(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 48'; return state; }
function render49(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 49'; return state; }
function render50(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 50'; return state; }
function render51(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 51'; return state; }
function render52(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 52'; return state; }
function render53(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 53'; return state; }
function render54(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 54'; return state; }
function render55(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 55'; return state; }
function render56(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 56'; return state; }
function render57(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 57'; return state; }
function render58(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 58'; return state; }
function render59(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 59'; return state; }
function render60(state) { document.getElementById('app').textContent = 'route ' + state.route + ' item 60'; return state; }
render1({route: location.pathname});
