## 1
`*.jpg` means every name that ends in .jpg. `mv FILES FOLDER/` moves files into a folder.
## 2
Standing in the sandbox: `mv *.jpg photos/` — check with `ls` and `ls photos`.
## 3
    cd ~/.k8s-wargame/shell/sandbox
    mv *.jpg photos/
