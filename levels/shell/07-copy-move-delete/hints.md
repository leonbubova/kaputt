## 1
Renaming is `mv OLD NEW`. Copying into a folder is `cp FILE FOLDER/`. Deleting is `rm FILE`.
## 2
Three commands, in the sandbox: `mv draft.txt final.txt`, then `cp final.txt archive/`, then `rm old-junk.txt`.
## 3
    cd ~/.k8s-wargame/shell/sandbox
    mv draft.txt final.txt
    cp final.txt archive/
    rm old-junk.txt
