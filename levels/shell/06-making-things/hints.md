## 1
`mkdir NAME` creates a folder, `touch NAME` creates an empty file. A path like `project/notes.txt` means "notes.txt inside project".
## 2
Standing in the sandbox: first `mkdir project`, then `touch project/notes.txt`.
## 3
    cd ~/.k8s-wargame/shell/sandbox
    mkdir project
    touch project/notes.txt
