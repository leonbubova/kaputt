## 1
A command is a line you type, finished with Enter. `echo WORDS` makes the computer print WORDS back to you.
## 2
`echo hello > SOMEFILE` prints into the file instead of the screen. The file here is `~/.k8s-wargame/shell/sandbox/hello.txt`.
## 3
    echo hello > ~/.k8s-wargame/shell/sandbox/hello.txt
