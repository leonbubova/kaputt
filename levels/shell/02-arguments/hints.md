## 1
Everything after the command name is an argument. `echo` prints all its arguments, separated by spaces.
## 2
`echo WORD1 WORD2 WORD3 > FILE` — three arguments, then `>` and the file `~/.k8s-wargame/shell/sandbox/colors.txt`.
## 3
    echo red green blue > ~/.k8s-wargame/shell/sandbox/colors.txt
