# ticket: daily digest at 09:00 Berlin
"Add a scheduled task `daily-digest` that runs every day at 09:00 **Europe/Berlin** (also after the DST switch). It should return `{ ranAt, timezone }` from the schedule payload." Goal: the task is registered with that declarative schedule.
