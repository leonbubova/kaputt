# import progress bar stuck at 0 %
"The dashboard progress bar for `import-csv` (reads run metadata `progress` and `rows`) stays at 0 % / 0 rows until the run completes. The run itself imports everything." Goal: after the run, metadata shows `progress: 100` and `rows: <all rows>`, and it was updated along the way.
