# where did the vendor installer put license.key?
Ticket: "The acme installer dropped a file called `license.key` somewhere under `/var`. Nobody wrote down where. Find it and write its full path to `/answer`." (Ignore backups — we want the live `license.key`.)
Goal: `/answer` contains the absolute path of `license.key`.
