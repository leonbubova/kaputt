# how many 500s did we serve yesterday?
Ticket: "Management wants a number: how many requests in `/var/log/app/access.log` got HTTP status `500`? Write just the number to `/answer`." (Status is the field after the quoted request. Only 500 — not 502, not 503.)
Goal: `/answer` contains the count.
