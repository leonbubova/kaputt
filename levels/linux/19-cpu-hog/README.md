# box is sluggish, load average pinned at 1
"Everything on `prod-app-01` feels slow, load is stuck at 1.0 even with nothing deployed. Monitoring shows one core at 100% for hours."
Goal: no process is eating the CPU any more (nothing above 50% CPU).
