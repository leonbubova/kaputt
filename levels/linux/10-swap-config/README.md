# roll out the new app.conf, keep the old one
Ticket: "The new config was uploaded to `/tmp/upload/app.conf.new`. Put it in place as `/etc/app/app.conf`. Keep the current one as `/etc/app/app.conf.bak` in case we need to roll back."
Goal: `/etc/app/app.conf` has the new content, `/etc/app/app.conf.bak` has the old content.
