# which client hammers us the most?
Ticket: "From `/var/log/app/access.log`, find the client IP (first field) with the most requests and write only that IP to `/answer`."
Goal: `/answer` contains the most frequent client IP.
