# report tool: "bad interpreter: No such file or directory"
"Someone edited `/usr/local/bin/report` on their laptop and copied it back. Now it says `/bin/bash: bad interpreter`. `/bin/bash` exists, we checked."
Goal: `report` runs and prints the user count line. Keep the script's logic.
