# alice and bob join the new audit group
Ticket: "Compliance needs a group `audit` with alice and bob as members. Add the membership on top of what they have — last time someone did this bob fell out of `dev` and lost access to the share. Primary groups stay as they are."
Goal: `id alice` and `id bob` both list `audit`; alice is still in `dev` and `operators`, bob still in `dev`; primary groups unchanged.
