apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-html
  labels:
    app: {{ .Release.Name }}
data:
  index.html: |
    <h1>{{ .Release.Name }}</h1>
    <p>env={{ .Values.environment }} domain={{ .Values.domain | default "localhost" }}</p>
