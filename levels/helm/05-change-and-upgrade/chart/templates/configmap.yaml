apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-html
  labels:
    app: {{ .Release.Name }}
data:
  index.html: |
    <h1>{{ .Values.page.title }}</h1>
    <p>env={{ .Values.environment }}</p>
