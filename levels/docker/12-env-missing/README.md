# api container dies on boot
"`wg-api` (image `wg-api:1.0`, built from `~/.k8s-wargame/docker/12-env-missing/`) exits right after start. Worked on my laptop."
Goal: `wg-api` runs from `wg-api:1.0` and stays up. Don't change the app code or the image.
