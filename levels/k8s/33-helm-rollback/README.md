# helm upgrade went wrong
"Release `shop` was upgraded to chart values from `prod.yaml` and now it's broken. Nobody remembers what changed." Goal: release `shop` is healthy (status deployed, pod available, page reachable via `http://shop.wg.svc/`). Bonus: find the diff first.
