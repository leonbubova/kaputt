# run a pod
Ticket: "Before we build anything, I want to see one nginx running in namespace `wg`."
Create a pod named `hello` with image `nginx:1.27-alpine`. Goal: the pod is `Running` with exactly that image.
