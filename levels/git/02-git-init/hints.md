## 1
A repository is just a folder with a `.git` inside. `git init` creates that `.git` in the folder you stand in.
## 2
First `cd ~/.k8s-wargame/git/repo`, then `git init`. Check with `ls -a`.
## 3
    cd ~/.k8s-wargame/git/repo
    git init
