# commit the bugfix, not the half-written notes
"Two files are modified in `~/.k8s-wargame/git/repo`: `app.py` (a finished fix) and `notes.md` (unfinished). Ship the fix now."
Goal: exactly one new commit containing only the `app.py` change; `notes.md` stays modified in the working tree, uncommitted.
