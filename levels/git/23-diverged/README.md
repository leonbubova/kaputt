# push rejected: "Updates were rejected because the tip of your current branch is behind"
"A colleague pushed to `main` while I was committing. Policy: no merge commits on `main`." Repo: `~/.k8s-wargame/git/repo`, remote `origin` is fine.
Goal: remote `main` contains both the colleague's commit and yours, history linear, local and remote identical.
