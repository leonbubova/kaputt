# bring feature/i18n into main — main has moved on
"`feature/i18n` is approved. Meanwhile a hotfix landed on `main`. Get the branch in; on this repo we keep merge commits so the branch stays visible in history." Repo: `~/.k8s-wargame/git/repo`.
Goal: `main`'s tip is a merge commit with the old `main` tip and `feature/i18n` as parents; both `hotfix.py` and `i18n.py` are on `main`; HEAD on `main`, clean tree.
