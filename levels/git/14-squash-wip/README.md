# three "wip" commits need to become one before review
"`feature/avatar` has `wip`, `wip 2`, `fix` — reviewer refuses to look until it's one commit with a real message." Repo: `~/.k8s-wargame/git/repo`.
Goal: `feature/avatar` is exactly one commit on top of `main` with the same final content and a proper message (not `wip`).
