# hotfix needed on main while I'm mid-edit
"I'm halfway through `report.py` on `feature/report` — not ready to commit. Release needs `VERSION` bumped to `1.4.1` on `main` right now. Afterwards my half-done edit must be back in `report.py` on `feature/report`." Repo: `~/.k8s-wargame/git/repo`.
Goal: `main` has one new commit with `VERSION` = `1.4.1`; `feature/report` has no new commit; HEAD is on `feature/report` with the in-progress `report.py` edit uncommitted; the stash list is empty.
