# stash dropped, hours of writing gone
"I stashed my draft of chapter 1 in `draft.md`, then hit `git stash drop` thinking it was already applied. `git stash list` is empty." Repo: `~/.k8s-wargame/git/repo`.
Goal: `draft.md` in the working tree contains the chapter 1 text again (committing it is optional).
