# push fails: "does not appear to be a git repository"
"After the server move nobody can push `shop-api`. One commit is waiting locally." Repo: `~/.k8s-wargame/git/repo`. The new location of the central repo is `~/.k8s-wargame/git/origin.git`.
Goal: `git push` works and the remote's `main` has the local commit.
