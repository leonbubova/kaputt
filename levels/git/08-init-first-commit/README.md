# new project, not under version control yet
"`shop-api` lives in `~/.k8s-wargame/git/repo` — two files, no git. Put it under version control." 
Goal: the directory is a git repository whose first commit contains `README.md` and `app.py`; nothing left uncommitted.
