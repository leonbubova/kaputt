# "You are in 'detached HEAD' state" — and there's a commit on it
"I checked out tag `v1.0` to test the timeout fix, committed it, and now git says the commit will be lost if I switch away." Repo: `~/.k8s-wargame/git/repo`.
Goal: that commit lives on a branch named `hotfix/timeout`; HEAD is attached to a branch again.
