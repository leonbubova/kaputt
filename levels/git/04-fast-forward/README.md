# bring the finished branch into main
"`feature/greeting` is reviewed and approved. Get it into `main`." Repo: `~/.k8s-wargame/git/repo`. `main` has not moved since the branch was created.
Goal: `main` points at the same commit as `feature/greeting`; no extra merge commit; HEAD on `main`.
