# publish the repo to the team's server
"The central bare repo is ready at `~/.k8s-wargame/git/origin.git` (empty). Our repo `~/.k8s-wargame/git/repo` has no remote yet."
Goal: remote `origin` points at that path and `main` is pushed, so the remote's `main` equals ours.
