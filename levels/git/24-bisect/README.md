# ./test.sh fails on main, passed two weeks ago
"20 commits since the last green run. Nobody knows which one broke `./test.sh`." Repo: `~/.k8s-wargame/git/repo`.
Goal: write the full hash of the FIRST commit where `./test.sh` fails into `~/.k8s-wargame/git/answer`. Leave the repo on `main` afterwards.
