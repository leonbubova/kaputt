# last commit has a garbage message
"Someone committed `login.py` to `shop-api` with the message `asdfasdf`. Review is blocked." Repo: `~/.k8s-wargame/git/repo`.
Goal: the last commit says `Add login endpoint` — same content, same parent, nothing else changes.
