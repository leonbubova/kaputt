# branch deleted with -D, never merged
"I cleaned up branches and force-deleted `feature/export`. It had two unmerged commits — the CSV export. Never pushed." Repo: `~/.k8s-wargame/git/repo`.
Goal: `feature/export` exists again with both commits and the same content as before.
