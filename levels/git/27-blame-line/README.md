# who set the worker timeout?
"`worker.py` has `TIMEOUT = 30` and the ops team wants to talk to whoever chose that value." Repo: `~/.k8s-wargame/git/repo`.
Goal: write the author name (as in the commit) of the commit that introduced the `TIMEOUT = 30` line into `~/.k8s-wargame/git/answer`.
