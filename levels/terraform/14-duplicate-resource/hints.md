## 1
Two resources of the same type must have different names (the second label).
## 2
Both blocks are `local_file "app"`. Rename one.
## 3
Rename the second to `local_file "app2"` (keep its own filename).
