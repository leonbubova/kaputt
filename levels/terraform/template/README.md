# wargame terraform play area
Work here. `terraform init` / `plan` / `apply` — providers come from a local mirror, no internet.
Generated files land in `out/`. State is the local `terraform.tfstate`.
