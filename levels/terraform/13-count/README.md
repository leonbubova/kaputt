# spin up three node files, not one
"We need three files `out/node-0.txt`, `out/node-1.txt`, `out/node-2.txt`, each containing `node <index>`. Use `count` — don't copy-paste three blocks."
Goal: after apply, the three files exist with the right content, from a single counted resource.
